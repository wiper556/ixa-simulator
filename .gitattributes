* text=auto eol=lf

# 調査ログの証拠(取得した本文)は**1バイトも変えてはいけない**。
# 監査が保存本文の SHA256 を突き合わせるので、改行を LF に直されると
# 取り出したツリーでハッシュが合わなくなり「情報源が足りない」と鳴る
# (2026-09-02、ixawiki と ixagno の本文が CRLF を含んでいて発覚)。
tools/research_cache/** -text
