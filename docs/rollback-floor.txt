# 巻き戻しの下限(RULE-OPERATION.md「巻き戻しの範囲」/ E-10)
#
# ユーザーが最後に「ここまでは確認した」と言った時点のコミット。
# `tools/rollback_reviewed.py --since <起点>` は、これより新しい起点を拒否する。
#
# なぜ要るか: 重い違反の巻き戻しで起点を選ぶのは巻き戻す本人なので、
# `--since HEAD` と書けば対象を0件にできてしまう(2026-08-12に実証済み)。
# 狭く取った誤りは検出できないため、下限を外に置く。
#
# **この行を書き換えてよいのは、ユーザーが確認したと明言したときだけ。**
# 書き換えるときは、いつ・誰が確認したかをコミットメッセージに残す。
#
# 形式: 先頭の空行でもコメントでもない行の1語目をコミットとして読む。

e56b80b  # 2026-08-12 セッション開始時点。これ以降の作業はまだユーザー確認を受けていない
